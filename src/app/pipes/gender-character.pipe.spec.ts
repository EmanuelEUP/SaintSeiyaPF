import { GenderCharacterPipe } from './gender-character.pipe';

describe('GenderCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
