import { SearchCharacterPipe } from './search-character.pipe';

describe('SearchCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
