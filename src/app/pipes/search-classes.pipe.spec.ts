import { SearchClassesPipe } from './search-classes.pipe';

describe('SearchClassesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchClassesPipe();
    expect(pipe).toBeTruthy();
  });
});
